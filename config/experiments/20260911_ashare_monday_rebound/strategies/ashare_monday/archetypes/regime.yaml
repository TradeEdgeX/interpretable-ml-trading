allowed_regimes:
  always:
    description: A-share Monday rebound — no regime gate
    match: any
    rules:
      - feature: atr
        operator: ">"
        value: 0
allowed_sides:
  - long
reject_neutral: false
side_mask:
  enabled: false
