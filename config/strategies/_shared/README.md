# Shared fragments (`config/strategies/_shared`)

Strategy packs may include snippets from here. The public dummy is still
`config/strategies/ma_cross/`.

Build that pack's FeatureStore with `mlbot feature-store build`, not a
shared tree-training layer. There is no `mlbot train` in this extract.
