# ============================================================================
# ML Trading Bot Requirements
# 本地开发环境: Ubuntu 22.04 + Python 3.12
# Docker 环境:  Ubuntu 24.04 + Python 3.12 (见 Dockerfile.gpu)
# 核心包基于 numpy 1.26.4
# ============================================================================

# Core dependencies
numpy==1.26.4
scipy==1.11.4
pandas==2.1.4
scikit-learn==1.3.2

# Machine Learning
lightgbm>=3.3.0
xgboost>=2.0.0
catboost>=1.2.0
shap==0.43.0
optuna==3.4.0
hmmlearn>=0.3.0
imodels>=1.3.5  # Explainable ML (rule lists/sets) for exporting tree rules

# Technical Analysis
ta-lib>=0.4.0
PyWavelets>=1.5.0  # Wavelet transforms (1.5.0+ for Python 3.12)
statsmodels==0.14.1
arch>=6.0.0  # GARCH models
dtaidistance>=2.3.0  # DTW distance for time series similarity

# Data Processing
pyarrow==15.0.2
polars==0.20.31
pyyaml==6.0.2
cloudpickle==2.2.1

# Visualization
matplotlib==3.7.5
seaborn==0.12.2
plotly==5.18.0
bokeh==3.3.4

# Utilities
tqdm>=4.62.0
joblib==1.3.2
tabulate==0.9.0
psutil>=5.9.0
openpyxl==3.1.2
python-dotenv==1.0.0
PyPDF2==3.0.1
click>=8.0.0
jinja2>=3.1.0

# Exchange/Trading APIs
ccxt>=4.5.0
python-binance>=1.0.37  # WS queue is per-instance max_queue_size (see websocket_client); keep local == prod
python-socks[asyncio]>=2.8.0
websockets==12.0
aiohttp>=3.9.0
websocket-client>=1.6.0  # sync fallback for WebSocket (used in thread bridge)
akshare>=1.14.0  # optional: mlbot data download-ashare (A-share court examples only)
baostock>=0.8.8  # optional: full-market A-share qfq + PIT universe (cohort_hold)

# Reinforcement Learning (local research only — not in requirements-ci.txt)
# gymnasium==0.29.1
# stable-baselines3==2.3.0  # pulls torch+CUDA; install locally if needed

# Backtesting/Trading
# nautilus-trader>=1.0.0  # Conflict with fsspec/msgspec, install separately
vectorbt>=0.26.0
alphalens-reloaded==0.4.5

# Optimization
cvxpy>=1.4.0

# JIT Compilation (Python 3.12 requires pre-release)
numba>=0.60.0
llvmlite>=0.43.0

# Web API / Business Console
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx2>=0.1

# Deep Learning (torch/mamba-ssm not needed for CI or live Docker image)
# torch>=2.0.0
# mamba-ssm>=1.0.0
# causal-conv1d>=1.2.0
# tensorboard==2.15.1

# Monitoring
prometheus_client>=0.20.0

# Testing
pytest>=7.0.0
