# ============================================================================
# CI / live-critical test deps only (GitHub Actions Job 0b + test.yml)
# ============================================================================
# NOT the research kitchen sink. Full ML/RL stack lives in requirements.txt.
# Suite: scripts/run_live_critical_tests.sh (OMS / live engines / CMS / mock).
# Do not add torch, stable-baselines3, vectorbt, alphalens, catboost, etc.
# ============================================================================

# Core
numpy==1.26.4
pandas==2.1.4
pyarrow==15.0.2
pyyaml==6.0.2
python-dotenv==1.0.0
joblib==1.3.2
psutil>=5.9.0

# Exchange / networking
ccxt>=4.5.0
python-binance>=1.0.37
python-socks[asyncio]>=2.8.0
websockets==12.0
aiohttp>=3.9.0
websocket-client>=1.6.0
requests>=2.31.0

# CMS / API surfaces touched by live-critical
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx2>=0.1
pydantic>=2.9.0
prometheus_client>=0.20.0

# Testing
pytest>=7.0.0
